import java.util.Vector;


public class Travel {
	//ATTRIBUTES
	private Vector<int> path = new Vector();
	
	//METHODES
	public Vector<int> getPath(){
		return(this.path);
	}
	
	public int calculLength(Map map){
		
		int length = 0;
		int a;
		int b;
		
		for(int i = 0; i < this.path.length() - 1; i++){
			a = Integer.parseInt(this.path[i]);
			b = Integer.parseInt(this.path[i + 1]);
			
			
			length += 
		}
		
		
		return 0;
	}
}
